apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "otus-user-app.fullname" . }}-test-connection"
  labels:
    {{- include "otus-user-app.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "otus-user-app.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
