apiVersion: v1
kind: Service
metadata:
  name: {{ include "otus-user-app.fullname" . }}
  labels:
    {{- include "otus-user-app.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "otus-user-app.selectorLabels" . | nindent 4 }}
